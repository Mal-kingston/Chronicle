<UserControl x:Class="Chronicle.TabContentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chronicle"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{x:Static local:TabContentDesignModel.Instance}">
    
    <Grid Grid.Row="1" Background="{StaticResource DarkBrush}">
        <Grid.RowDefinitions>
            <!--Title-->
            <RowDefinition Height="50" />
            <!--Content-->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--Title-->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal">
            <TextBlock VerticalAlignment="Bottom" Text="{Binding TitleLabel}" Margin="50 0 0 0" 
                       FontFamily="{StaticResource JostFont}" FontSize="{StaticResource XSmall}" Foreground="{StaticResource GrayBrush}" />

            <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource GrayBrush}" 
                    Width="200" Margin="10 0 0 0" Padding="10 0 0 0" VerticalAlignment="Bottom">
                
                <TextBox  x:Name="Title" Background="Transparent" BorderThickness="0" MaxLength="32" 
                          Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                          local:IsFocusedAttachedProperties.Value="True"
                          Keyboard.KeyDown="TextBox_KeyDown" TextWrapping="NoWrap" 
                          FontFamily="{StaticResource JostFont}" FontSize="{StaticResource XSmall}" Foreground="{StaticResource GrayBrush}" 
                          CaretBrush="{StaticResource GrayBrush}"/>

            </Border>

        </StackPanel>

        <!--Content-->
        <TextBox Grid.Row="1" x:Name="MainContent" Background="Transparent" TextAlignment="Left" Foreground="{StaticResource GrayBrush}" TextWrapping="Wrap" Margin="0 20 0 0"
                 AcceptsTab="True" AcceptsReturn="True" BorderBrush="{x:Null}" BorderThickness="0" HorizontalScrollBarVisibility="Disabled"
                 FontFamily="{StaticResource JostFont}" FontSize="{StaticResource XSmall}" Padding="20 20 40 5" CaretBrush="{StaticResource GrayBrush}" 
                 Text="{Binding Content, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
    </Grid>

</UserControl>
